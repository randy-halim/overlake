{
  "devDependencies": {
    "@types/node": "^14.14.11",
    "nodemon": "^2.0.6",
    "typescript": "^4.1.2"
  },
  "main": "./index.js",
  "scripts": {
    "start": "tsc --watch",
    "build": "tsc && docker build .",
    "test": "echo \"Error: No unit testing is configured.\" && exit 1"
  },
  "dependencies": {
    "discord.js": "^11"
  }
}
